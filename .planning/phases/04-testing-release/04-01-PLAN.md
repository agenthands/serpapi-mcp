---
phase: 04-testing-release
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/search/search_test.go
  - internal/search/validation_test.go
  - internal/engines/engines_test.go
  - cmd/serpapi-mcp/main_test.go
autonomous: true
requirements: [TEST-01, TEST-02, TEST-05, TEST-06]

must_haves:
  truths:
    - "Search tool handles all edge cases: malformed JSON, empty params, non-object responses, compact on arrays"
    - "Validation rejects empty engine name, empty mode, and reports correct missing params"
    - "Engine RequiredParams() returns correct required fields for known engines"
    - "CLI helper functions (envOr, envIntOr, envBoolOr) handle all environment variable edge cases"
    - "All existing tests continue to pass"
  artifacts:
    - path: "internal/search/search_test.go"
      provides: "Extended search unit tests"
      contains: "TestSearchMalformedJSON"
    - path: "internal/search/validation_test.go"
      provides: "Extended validation unit tests"
      contains: "TestValidateEngineEmpty"
    - path: "internal/engines/engines_test.go"
      provides: "RequiredParams unit tests"
      contains: "TestRequiredParams"
    - path: "cmd/serpapi-mcp/main_test.go"
      provides: "CLI helper function tests"
      contains: "TestEnvOr"
  key_links:
    - from: "internal/search/search_test.go"
      to: "internal/search/search.go"
      via: "callSearchTool and setupTestServer"
      pattern: "func Test.*Search.*"
    - from: "internal/search/validation_test.go"
      to: "internal/search/validation.go"
      via: "ValidateEngine, ValidateMode, ValidateRequiredParams"
      pattern: "func TestValidate.*"
    - from: "cmd/serpapi-mcp/main_test.go"
      to: "cmd/serpapi-mcp/main.go"
      via: "envOr, envIntOr, envBoolOr"
      pattern: "func TestEnv.*"
---

<objective>
Harden unit tests for search tool, compact mode, engine resources, validation, and CLI helpers — filling edge-case gaps not covered by existing tests.

Purpose: Requirements TEST-01, TEST-02, TEST-05, TEST-06 specify comprehensive unit tests. Existing tests cover the happy paths and primary error cases, but miss edge cases (empty strings, non-object JSON, malformed input, CLI env var parsing). This plan closes those gaps.

Output: Extended test files with edge-case coverage; new main_test.go for CLI helpers.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-search-validation-observability/03-01-SUMMARY.md
@.planning/phases/03-search-validation-observability/03-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From internal/search/search.go:
```go
func RegisterSearchTool(srv *mcp.Server, logger *slog.Logger)
func callSearchTool(ctx context.Context, req *mcp.CallToolRequest) (*mcp.CallToolResult, error)
func toolError(code, message string) *mcp.CallToolResult
var serpapiBaseURL string
var compactRemoveFields []string  // ["search_metadata", "search_parameters", "search_information", "pagination", "serpapi_pagination"]
type searchInput struct {
    Params map[string]any `json:"params"`
    Mode   string         `json:"mode"`
}
```

From internal/search/validation.go:
```go
func ValidateEngine(engine string) error
func ValidateMode(mode string) error
func ValidateRequiredParams(engine string, params map[string]any) error
```

From internal/engines/engines.go:
```go
func LoadAndRegister(srv *mcp.Server, enginesDir string, logger *slog.Logger) (int, error)
func EngineNames() []string
func RequiredParams(engineName string) []string
```

From internal/server/auth.go:
```go
func APIKeyFromContext(ctx context.Context) string
func ContextWithAPIKey(ctx context.Context, key string) context.Context
```

From cmd/serpapi-mcp/main.go:
```go
func envOr(key, fallback string) string
func envIntOr(key string, fallback int) int
func envBoolOr(key string, fallback bool) bool
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add edge-case tests for search tool and validation</name>
  <files>internal/search/search_test.go, internal/search/validation_test.go</files>
  <read_first>internal/search/search_test.go, internal/search/search.go, internal/search/validation_test.go, internal/search/validation.go</read_first>
  <behavior>
    - TestSearchMalformedJSON: callSearchTool with invalid JSON `{"params":` returns IsError=true with "search_error" code
    - TestSearchEmptyParams: callSearchTool with params={} (no engine) defaults to google_light, returns success
    - TestSearchNullArguments: callSearchTool with nil Arguments does not crash (defaults to empty params, google_light, complete mode)
    - TestSearchCompactModeOnArrayResponse: mock server returns `[1,2,3]` (JSON array, not object), compact mode returns array unchanged (not object so no fields to remove)
    - TestSearchCompactModeAllFieldsRemoved: verify all 5 compactRemoveFields are absent from compact response
    - TestValidateEngineEmpty: ValidateEngine("") returns error containing "invalid_engine"
    - TestValidateModeEmpty: ValidateMode("") returns error containing "invalid_mode"
    - TestValidateModeCaseInsensitive: Verify "COMPLETE" or "Compact" are rejected (validation is case-sensitive, only lowercase accepted)
    - TestValidateRequiredParamsUnknownEngine: ValidateRequiredParams("nonexistent", params) returns nil (engine not found → skip validation, ValidateEngine catches it)
    - TestValidateRequiredParamsNilParams: ValidateRequiredParams with nil params map doesn't crash
  </behavior>
  <action>
RED phase — Write failing tests first:

In internal/search/search_test.go, add after existing tests:
1. TestSearchMalformedJSON — call callWithAPIKey with `{"params":` (truncated JSON). Assert IsError=true and Content contains "search_error".
2. TestSearchEmptyParams — call callWithAPIKey with `{"params":{},"mode":"complete"}`. Assert IsError=false (defaults to google_light, which has q as required, so this WILL fail validation — adjust: include "q":"test" in params and omit "engine"). Actually, params={} with default engine google_light requires "q". So test with `{"params":{"q":"test"}}` (no engine key) and verify the request is sent with engine=google_light.
3. TestSearchNullArguments — call callSearchTool(context.Background(), &mcp.CallToolRequest{Params: &mcp.CallToolParamsRaw{Name:"search", Arguments: nil}}). Assert no panic, returns error result with "missing_api_key" or "search_error" (since API key is missing).
4. TestSearchCompactModeOnArrayResponse — set up mock server returning `Content-Type: application/json` with body `[1,2,3]`. Call with mode=compact. Verify result is not error, and text content is `[1,2,3]` (array untouched).
5. TestSearchCompactModeAllFieldsRemoved — verify individually that each of the 5 compactRemoveFields (search_metadata, search_parameters, search_information, pagination, serpapi_pagination) are absent in compact mode response.

In internal/search/validation_test.go, add after existing tests:
1. TestValidateEngineEmpty — ValidateEngine(""). Assert error contains "invalid_engine".
2. TestValidateModeEmpty — ValidateMode(""). Assert error contains "invalid_mode".
3. TestValidateModeCaseSensitive — ValidateMode("COMPLETE"). Assert error (case-sensitive).
4. TestValidateRequiredParamsUnknownEngine — ValidateRequiredParams("nonexistent_engine_xyz", map[string]any{"q":"test"}). Assert nil (unknown engine → skip, ValidateEngine catches separately).
5. TestValidateRequiredParamsNilParams — ValidateRequiredParams("google_light", nil). Assert error or nil — check actual behavior by reading validation.go. If it panics on nil map, we need a fix too.

GREEN phase — Run `go test ./internal/search/...` — if any tests fail due to bugs in production code (not just test bugs), fix the production code minimally to pass.

REFACTOR — Clean up test helpers, deduplicate mock setup if needed.
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/agenthands/serpapi-mcp && go test ./internal/search/... -v -count=1 2>&1 | tail -20</automated>
  </verify>
  <done>All new and existing search and validation tests pass. Edge cases for empty strings, malformed JSON, non-object responses, and nil params are covered.</done>
  <acceptance_criteria>
    - internal/search/search_test.go contains "TestSearchMalformedJSON"
    - internal/search/search_test.go contains "TestSearchCompactModeOnArrayResponse"
    - internal/search/search_test.go contains "TestSearchCompactModeAllFieldsRemoved"
    - internal/search/validation_test.go contains "TestValidateEngineEmpty"
    - internal/search/validation_test.go contains "TestValidateModeEmpty"
    - go test ./internal/search/... exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add engine accessor tests and CLI helper tests</name>
  <files>internal/engines/engines_test.go, cmd/serpapi-mcp/main_test.go</files>
  <read_first>internal/engines/engines_test.go, internal/engines/engines.go, cmd/serpapi-mcp/main.go</read_first>
  <behavior>
    - TestRequiredParamsForEngineWithRequired: LoadAndRegister with engine having {"q":{"required":true}}, then RequiredParams("google_light") returns ["q"]
    - TestRequiredParamsForEngineWithNoRequired: LoadAndRegister with engine having no required params, then RequiredParams returns nil or empty
    - TestEngineNamesSorted: LoadAndRegister multiple engines, then EngineNames() returns sorted list
    - TestEnvOrSetEnv: os.Setenv("TEST_KEY", "value"), envOr("TEST_KEY", "default") returns "value"
    - TestEnvOrUnsetEnv: envOr("NONEXISTENT_KEY_XYZ", "fallback") returns "fallback"
    - TestEnvOrEmptyEnv: os.Setenv("TEST_EMPTY", ""), envOr("TEST_EMPTY", "fallback") returns "fallback" (empty string treated as unset)
    - TestEnvIntOrValid: os.Setenv("TEST_PORT", "8080"), envIntOr("TEST_PORT", 8000) returns 8080
    - TestEnvIntOrInvalid: os.Setenv("TEST_BAD", "abc"), envIntOr("TEST_BAD", 8000) returns 8000 (fallback)
    - TestEnvIntOrUnset: envIntOr("NONEXISTENT_KEY_XYZ", 8000) returns 8000
    - TestEnvBoolOrTruthyValues: "1", "true", "yes" (case-insensitive) return true
    - TestEnvBoolOrFalsyValues: "0", "false", "no", empty string return fallback
    - TestEnvBoolOrUnset: envBoolOr("NONEXISTENT_KEY_XYZ", true) returns true (fallback)
  </behavior>
  <action>
RED phase — Write failing tests:

In internal/engines/engines_test.go, add:
1. TestRequiredParamsForEngineWithRequired — Create temp dir with google_light.json having `{"engine":"google_light","params":{"q":{"required":true,"description":"query"},"hl":{"required":false,"description":"language"}}}`. LoadAndRegister, then assert RequiredParams("google_light") returns []string{"q"} (sorted).
2. TestRequiredParamsForEngineWithNoRequired — Create temp dir with engine having no required params. Assert RequiredParams returns nil or empty slice.
3. TestEngineNamesReturnsSortedCopy — Load 3 engines (alpha, charlie, bravo). Assert EngineNames() returns ["alpha","bravo","charlie"]. Modify the returned slice and call EngineNames() again to verify it returns a fresh copy (not the same slice reference).

In cmd/serpapi-mcp/main_test.go, create new file with package main:
1. TestEnvOrSetEnv — os.Setenv("TEST_GSD_ENV_OR", "fromenv"); defer os.Unsetenv. Call envOr("TEST_GSD_ENV_OR", "default"). Assert "fromenv".
2. TestEnvOrUnsetEnv — os.Unsetenv("TEST_GSD_ENV_OR_NONE"). Call envOr("TEST_GSD_ENV_OR_NONE", "fallback"). Assert "fallback".
3. TestEnvOrEmptyEnv — os.Setenv("TEST_GSD_ENV_OR_EMPTY", ""); defer os.Unsetenv. Call envOr("TEST_GSD_ENV_OR_EMPTY", "fallback"). Assert "fallback" (per envOr implementation: empty string → fallback).
4. TestEnvIntOrValid — os.Setenv("TEST_GSD_ENV_INT", "9090"); defer os.Unsetenv. Call envIntOr("TEST_GSD_ENV_INT", 8000). Assert 9090.
5. TestEnvIntOrInvalid — os.Setenv("TEST_GSD_ENV_INT_BAD", "notanumber"); defer os.Unsetenv. Call envIntOr("TEST_GSD_ENV_INT_BAD", 8000). Assert 8000.
6. TestEnvIntOrUnset — Call envIntOr("TEST_GSD_ENV_INT_NONE_XYZ", 3000). Assert 3000.
7. TestEnvBoolOrTruthy — For each of "1", "true", "True", "TRUE", "yes", "Yes", "YES": os.Setenv then call envBoolOr, assert true.
8. TestEnvBoolOrFalsy — For each of "0", "false", "no", "": os.Setenv then call envBoolOr with fallback=false, assert false.
9. TestEnvBoolOrUnset — Call envBoolOr("TEST_GSD_ENV_BOOL_NONE_XYZ", true). Assert true.

GREEN phase — Run tests. envOr/envIntOr/envBoolOr are already implemented in main.go, so tests should pass after implementation verification. If any function behavior differs from test expectations, adjust tests to match actual implementation (check the code first).

REFACTOR — Clean up, ensure all env var keys are unique to avoid test interference.
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/agenthands/serpapi-mcp && go test ./internal/engines/... ./cmd/serpapi-mcp/... -v -count=1 2>&1 | tail -20</automated>
  </verify>
  <done>All new and existing engine tests pass. main_test.go created with envOr/envIntOr/envBoolOr coverage. EngineNames and RequiredParams have direct unit test coverage.</done>
  <acceptance_criteria>
    - internal/engines/engines_test.go contains "TestRequiredParamsForEngineWithRequired"
    - internal/engines/engines_test.go contains "TestEngineNamesReturnsSortedCopy"
    - cmd/serpapi-mcp/main_test.go exists and contains "TestEnvOrSetEnv"
    - cmd/serpapi-mcp/main_test.go contains "TestEnvBoolOrTruthy"
    - go test ./internal/engines/... ./cmd/serpapi-mcp/... exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
go test ./... -count=1 passes all tests (existing + new)
go test ./internal/search/... ./internal/engines/... ./cmd/serpapi-mcp/... -count=1 passes
</verification>

<success_criteria>
- All existing + new search and validation tests pass
- EngineNames() and RequiredParams() have direct unit tests
- cmd/serpapi-mcp/main_test.go created with envOr/envIntOr/envBoolOr tests
- go test ./... passes with no failures
</success_criteria>

<output>
After completion, create `.planning/phases/04-testing-release/04-01-SUMMARY.md`
</output>