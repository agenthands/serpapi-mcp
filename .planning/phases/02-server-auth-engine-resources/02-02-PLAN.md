---
phase: 02-server-auth-engine-resources
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - internal/server/auth.go
  - internal/server/auth_test.go
  - internal/server/server.go
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03]

must_haves:
  truths:
    - "Requests without a valid API key are rejected with 401 and JSON error body"
    - "API key extracted from URL path /{KEY}/mcp and forwarded with path stripped to /mcp"
    - "API key extracted from Authorization: Bearer {KEY} header"
    - "Auth middleware composes with MCP handler via standard Go http.Handler wrapping"
  artifacts:
    - path: "internal/server/auth.go"
      provides: "API key auth middleware with path-based and Bearer header extraction"
      min_lines: 40
    - path: "internal/server/auth_test.go"
      provides: "Tests for both auth methods and rejection cases"
      min_lines: 50
  key_links:
    - from: "internal/server/auth.go"
      to: "internal/server/server.go"
      via: "authMiddleware wrapping MCPServer's handler chain"
      pattern: "authMiddleware|AuthMiddleware"
    - from: "internal/server/auth.go"
      to: "net/http"
      via: "http.Handler wrapping pattern"
      pattern: "http\\.Handler"
---

<objective>
Create unified API key auth middleware that handles both path-based and Bearer header authentication, matching the Python server's auth behavior.

Purpose: Secure the MCP endpoint so only requests with valid API keys can proceed, maintaining backward compatibility with the Python server's auth model.
Output: Auth middleware that extracts API keys from URL path or Bearer header, strips the key segment from the path, and passes the key to downstream handlers via context.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-server-auth-engine-resources/02-CONTEXT.md
@.planning/phases/02-server-auth-engine-resources/02-01-SUMMARY.md
@legacy/src/server.py

<interfaces>
<!-- Key types from Plan 01 that this plan depends on -->

From internal/server/server.go (created by Plan 01):
```go
// Config holds server configuration
type Config struct {
    Host        string
    Port        int
    CorsOrigins string
}

// MCPServer holds the MCP server and HTTP server
type MCPServer struct {
    // server, handler, config, httpServer, logger fields
}

// NewMCPServer creates a configured MCP server
func NewMCPServer(cfg Config, logger *slog.Logger) *MCPServer

// Run starts the server with graceful shutdown
func (s *MCPServer) Run(ctx context.Context) error
```

Context key pattern for API key passing (D-02):
```go
// Custom context key type to avoid collisions
type contextKey string
const apiKeyContextKey contextKey = "apiKey"
```

Python reference for auth behavior (legacy/src/server.py lines 140-173):
- ApiKeyMiddleware checks /healthcheck path (skips auth)
- Extracts from Authorization: Bearer header first
- Falls back to URL path /{KEY}/mcp pattern
- Strips key segment from path for downstream
- Returns 401 JSON {"error": "Missing API key..."} on missing key
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create auth middleware with path-based and Bearer header key extraction</name>
  <files>internal/server/auth.go, internal/server/auth_test.go</files>
  <behavior>
    - Test 1: Request to /mcp with Authorization: Bearer testkey123 passes through with API key "testkey123" in context
    - Test 2: Request to /testkey123/mcp passes through with API key "testkey123" in context and path rewritten to /mcp
    - Test 3: Request to /mcp without any auth returns 401 with JSON body {"error": "Missing API key..."}
    - Test 4: Request to /health passes through without auth check (healthcheck is exempt)
    - Test 5: Bearer header takes priority over path-based key when both are present
    - Test 6: Path /somekey/mcp strips to /mcp and extracts "somekey" as API key
  </behavior>
  <action>
    1. Create `internal/server/auth.go` implementing the unified auth middleware per D-01:
       - Define custom context key type: `type contextKey string` with `const apiKeyContextKey contextKey = "apiKey"` per D-02
       - `func APIKeyFromContext(ctx context.Context) string` — extracts API key from context, helper for downstream use
       - `func authMiddleware(next http.Handler) http.Handler` — returns http.Handler that:
         - Checks if path is exactly "/health" — skip auth, pass through (per D-10)
         - Extracts API key:
           a. Check Authorization header for "Bearer {KEY}" format per AUTH-02
           b. If no Bearer header, check URL path for pattern /{KEY}/mcp per AUTH-01/D-03
           c. For path-based: extract key segment, rewrite path by stripping /{KEY} prefix
         - If no API key found: return 401 with JSON `{"error": "Missing API key. Use path format /{API_KEY}/mcp or Authorization: Bearer {API_KEY} header"}` per D-04
         - If API key found: store in context via context.WithValue(r.Context(), apiKeyContextKey, apiKey)
         - For path-based auth, create new request with modified URL path (stripped key segment) and updated request scope per D-03
         - Call next.ServeHTTP with the new context/request
       - All functions must have complete type annotations

    2. Create `internal/server/auth_test.go` with:
       - TestBearerHeaderAuth: Authorization header "Bearer testkey123" → API key "testkey123" in context
       - TestBearerHeaderAuthWithPrefix: "Bearer my-api-key-456" → "my-api-key-456" extracted
       - TestPathBasedAuth: GET /{KEY}/mcp → key in context, path rewritten to /mcp
       - TestMissingAPIKey: GET /mcp without auth → 401 with JSON error body
       - TestHealthcheckExempt: GET /health → passes through without auth
       - TestBearerHeaderPriority: Both Bearer header and path key → Bearer key used (D-01)
       - TestInvalidBearerFormat: "Basic abc123" → 401 (not Bearer format)
       - All test helpers using httptest.NewRecorder and httptest.NewRequest
  </action>
  <read_first>
    - internal/server/server.go (from Plan 01, for Config and MCPServer types)
    - legacy/src/server.py (lines 140-173, Python ApiKeyMiddleware reference)
  </read_first>
  <acceptance_criteria>
    - internal/server/auth.go contains "func authMiddleware" returning http.Handler
    - internal/server/auth.go contains "func APIKeyFromContext" for context extraction
    - internal/server/auth.go contains custom context key type "apiKeyContextKey"
    - Auth error returns 401 status code with JSON body containing "Missing API key"
    - Path /{KEY}/mcp is rewritten to /mcp with key extracted
    - Bearer header "Authorization: Bearer {KEY}" extracts key
    - /health path is exempt from auth
    - go test ./internal/server/... -run TestAuth passes
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/agenthands/serpapi-mcp && go test ./internal/server/... -count=1 -timeout 30s</automated>
  </verify>
  <done>Auth middleware handles both path-based and Bearer header API key extraction, passes key via context, and returns 401 JSON for missing keys</done>
</task>

<task type="auto">
  <name>Task 2: Wire auth middleware into server handler chain</name>
  <files>internal/server/server.go</files>
  <action>
    Update `internal/server/server.go` to compose auth middleware into the handler chain per AUTH-03 and D-12:

    1. In the `Run` method (or `NewMCPServer`, wherever the handler chain is constructed), the wrapping order must be:
       - authMiddleware → corsMiddleware → mux (with /mcp and /health routes)
       This means auth runs first (rejects unauthenticated requests before CORS), then CORS, then routing.
       Actually, reconsidering: per D-12 "CORS middleware applied at the HTTP handler level before the MCP StreamableHTTPHandler" — and per Python's middleware order [RequestMetrics, ApiKey, CORS]. The Python order is: metrics → apikey → CORS → handler. In Go, we want CORS before auth so that preflight OPTIONS requests get CORS headers. But wait — auth exempts /health and requires auth for /mcp. Let's follow the Python pattern: auth first, CORS second.
       
       Actually, reconsidering again: CORS preflight (OPTIONS) requests don't have auth headers. If auth runs before CORS, OPTIONS requests to /mcp get a 401 before CORS can respond with headers. The standard Go pattern is: CORS → auth → handler. This way OPTIONS gets CORS headers, and authenticated requests go through both. This also matches D-12 "CORS middleware applied before the MCP StreamableHTTPHandler".
       
       Handler chain: corsMiddleware(corsCfg, authMiddleware(muxWithRoutes))
    
    2. The auth middleware needs to be applied selectively — skip /health, require auth for /mcp. This is already handled inside authMiddleware (Task 1 checks path and exempts /health).
    
    3. Ensure the mux routes are:
       - /mcp → StreamableHTTPHandler.ServeHTTP
       - /health → healthcheck handler (no auth needed)
       - /{KEY}/mcp → auth strips key and rewrites path → /mcp → StreamableHTTPHandler.ServeHTTP

    4. Update Config to allow disabling auth for testing (add `AuthDisabled bool` field — when true, authMiddleware is a pass-through). This is optional but good practice for unit testing.

    5. Add integration-level verification: create `internal/server/integration_test.go` (or extend auth_test.go) with:
       - Test that a request to /mcp without auth returns 401
       - Test that a request to /{KEY}/mcp with valid path returns 200-level response
       - Test that /health returns 200 without auth
       - Test that CORS headers are present on responses regardless of auth
  </action>
  <read_first>
    - internal/server/server.go (Plan 01 version, for handler chain setup)
    - internal/server/auth.go (Task 1, just created)
    - internal/server/cors.go (Plan 01, Task 2)
  </read_first>
  <acceptance_criteria>
    - internal/server/server.go handler chain is: CORS → Auth → mux routes
    - Request to /mcp without auth returns 401
    - Request to /{KEY}/mcp passes auth and reaches MCP handler
    - Request to /health bypasses auth and returns 200
    - CORS headers present on both authenticated and unauthenticated responses
    - go build ./cmd/serpapi-mcp compiles
    - go test ./internal/server/... passes
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/agenthands/serpapi-mcp && go build ./cmd/serpapi-mcp && go test ./internal/server/... -count=1 -timeout 30s</automated>
  </verify>
  <done>Auth middleware composed with CORS and MCP handler in a clean http.Handler chain, maintaining Python server compatibility for both path-based and Bearer header auth</done>
</task>

</tasks>

<verification>
1. `go build ./cmd/serpapi-mcp` compiles without errors
2. `go vet ./...` passes
3. `go test ./internal/server/... -count=1` passes all auth tests
4. URL path /{KEY}/mcp correctly extracts key and rewrites path
5. Authorization: Bearer header extraction works
6. Missing API key returns 401 with JSON error
7. /health endpoint exempt from auth
</verification>

<success_criteria>
- API key extracted from URL path /{KEY}/mcp (AUTH-01)
- API key extracted from Authorization: Bearer {KEY} header (AUTH-02)
- Auth middleware composed with StreamableHTTPHandler via http.Handler wrapping (AUTH-03)
- Auth errors return 401 with JSON error body matching Python format (D-04)
- Healthcheck exempt from auth
</success_criteria>

<output>
After completion, create `.planning/phases/02-server-auth-engine-resources/02-02-SUMMARY.md`
</output>