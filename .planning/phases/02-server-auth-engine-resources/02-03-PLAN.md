---
phase: 02-server-auth-engine-resources
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - internal/engines/engines.go
  - internal/engines/engines_test.go
  - cmd/serpapi-mcp/main.go
autonomous: true
requirements: [ENG-01, ENG-02, ENG-03, ENG-04, ENG-05]

must_haves:
  truths:
    - "serpapi://engines resource returns the list of all available engine names to MCP clients"
    - "serpapi://engines/{engine} resource returns the parameter schema for a specific engine"
    - "Server fails to start if engine JSON files are corrupt or missing"
    - "Engine schema generation remains via build-engines.py (not ported to Go)"
  artifacts:
    - path: "internal/engines/engines.go"
      provides: "LoadAndRegister function loading 107 engine JSON files and registering MCP resources, engines index resource handler"
      min_lines: 80
    - path: "internal/engines/engines_test.go"
      provides: "Tests for engine loading, schema serving, and validation"
      min_lines: 40
    - path: "cmd/serpapi-mcp/main.go"
      provides: "Updated main.go calling engines.LoadAndRegister"
      min_lines: 1
  key_links:
    - from: "cmd/serpapi-mcp/main.go"
      to: "internal/engines/engines.go"
      via: "engines.LoadAndRegister(mcpServer, enginesDir) call"
      pattern: "engines\\.LoadAndRegister"
    - from: "internal/engines/engines.go"
      to: "github.com/modelcontextprotocol/go-sdk/mcp"
      via: "server.AddResource for engines index and per-engine schemas"
      pattern: "server\\.AddResource|mcp\\.Resource"
    - from: "internal/engines/engines.go"
      to: "engines/*.json"
      via: "os.ReadDir and os.ReadFile loading JSON files"
      pattern: "os\\.ReadDir|os\\.ReadFile"
---

<objective>
Load all 107 engine JSON schemas into memory at startup, validate them, and register MCP resources for engine discovery (list and per-engine schemas).

Purpose: Enable MCP clients to discover available SerpApi engines and their parameter schemas through the standard MCP resource interface.
Output: Engine loading with fast startup validation, serpapi://engines resource listing all engines, and serpapi://engines/{engine} resources serving individual engine schemas.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-server-auth-engine-resources/02-CONTEXT.md
@.planning/phases/02-server-auth-engine-resources/02-01-SUMMARY.md
@legacy/src/server.py

<interfaces>
<!-- Key types from Plan 01 that this plan depends on -->

From go-sdk/mcp (v1.5.0):
```go
// Resource registration
func (s *Server) AddResource(r *Resource, h ResourceHandler)

// Resource struct
type Resource struct {
    Name        string `json:"name"`
    URI         string `json:"uri"`
    Description string `json:"description,omitempty"`
    MIMEType    string `json:"mimeType,omitempty"`
    // Meta, Annotations, Size, Title fields also available
}

// ResourceHandler returns resource contents
type ResourceHandler func(context.Context, *ReadResourceRequest) (*ReadResourceResult, error)

// ReadResourceRequest and ReadResourceResult types
type ReadResourceRequest struct { Params ReadResourceParams }
type ReadResourceResult struct { Content []ResourceContents }
```

From internal/server/server.go (Plan 01):
```go
// MCPServer holds the MCP server
type MCPServer struct {
    // contains *mcp.Server
}

// NewMCPServer creates a configured MCP server
func NewMCPServer(cfg Config, logger *slog.Logger) *MCPServer
```

Engine JSON structure (from engines/google_light.json):
```json
{
  "engine": "google_light",
  "params": {
    "q": {
      "required": true,
      "description": "...",
      "group": "search_query"
    },
    "location": {
      "type": "location",
      "description": "...",
      "group": "geographic_location"
    }
  }
}
```

Python reference pattern (legacy/src/server.py lines 27-77):
- ENGINES_DIR resolved relative to source file parent's parent
- _get_engine_files() returns sorted list of *.json in ENGINES_DIR
- engines_index() returns dict with count, engines list, resources URIs, schema info
- _engine_resource_factory() creates per-engine Resource with lazy loading
- Engine names validated: only [a-z0-9_]+ allowed
- All engines registered at startup via mcp.add_resource()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create engine loading with startup validation and MCP resource registration</name>
  <files>internal/engines/engines.go, internal/engines/engines_test.go, cmd/serpapi-mcp/main.go</files>
  <behavior>
    - Test 1: LoadAndRegister with valid engines directory returns no error and registers engine count
    - Test 2: Reading serpapi://engines resource returns JSON with count, engines list, and resource URIs
    - Test 3: Reading serpapi://engines/google_light resource returns the full JSON schema for google_light engine
    - Test 4: LoadAndRegister with missing/invalid engines directory returns error (fail-fast)
    - Test 5: LoadAndRegister with corrupt JSON file returns parse error (fail-fast per ENG-04)
    - Test 6: Engine name validation: only [a-z0-9_]+ filenames are accepted, others produce warning and are skipped
  </behavior>
  <action>
    1. Create `internal/engines/engines.go` with:
       - `var EngDir string` initialized in init() to `Path(__file__).resolve().parents[1] / "engines"` equivalent in Go — use the path relative to the binary. In Go, resolve this by embedding or using a build-time variable. Best approach: use the same pattern as Python — resolve relative to the executable. However, Go binaries don't have `__file__`. Use a `DefaultEnginesDir` function that resolves relative to the working directory or accept enginesDir as parameter.
       
       Actually, per AGENTS.md: "ENGINES_DIR is resolved relative to src/server.py (Path(__file__).resolve().parents[1] / 'engines'), so it points to the repo-root engines/ directory". In Go, the cleanest approach is to pass the engines directory as a CLI flag/env var with a default that uses the executable's directory or current working directory.
       
       Final design for engines directory resolution per D-05/D-06:
       - `func LoadAndRegister(srv *mcp.Server, enginesDir string, logger *slog.Logger) (int, error)` — returns engine count
       - LoadAndRegister reads enginesDir/*.json, validates each file:
         a. Skip files not matching [a-z0-9_]+.json pattern (log warning)
         b. Parse JSON, verify it has an "engine" field matching the filename stem
         c. Store parsed schemas in an in-memory map[string]json.RawMessage
         d. If any JSON parse fails: return error immediately (fail-fast per ENG-04)
       - Register serpapi://engines resource returning JSON: {"count": N, "engines": [...], "resources": [...]}
       - Register serpapi://engines/{name} for each engine using server.AddResource with the engine's full JSON as content
       - All functions must have complete type annotations

    2. Update `cmd/serpapi-mcp/main.go`:
       - Add --engines-dir CLI flag (default: "engines" relative to working directory, overridable)
       - Add ENGINES_DIR env var support (CLI flag takes precedence per D-07 pattern)
       - Call engines.LoadAndRegister(mcpServer, enginesDir, logger) after NewMCPServer
       - If LoadAndRegister returns error, log.Fatal (fail-fast per ENG-04)
       - Update startup log message to include engine count: "serpapi-mcp started on {host}:{port} with {N} engines" per D-09/OBS-03

    3. Create `internal/engines/engines_test.go` using a temporary directory with test JSON files:
       - TestLoadAndRegister_ValidEngines: create temp dir with 2 valid engine JSON files → no error, count 2
       - TestLoadAndRegister_MissingDir: pass non-existent directory → error returned (ENG-04)
       - TestLoadAndRegister_CorruptJSON: create temp dir with invalid JSON → error returned (ENG-04)
       - TestLoadAndRegister_InvalidName: JSON file with hyphen in name → skipped with warning
       - TestEnginesIndexResource: read serpapi://engines → JSON with count, engines list, resource URIs
       - TestEngineSchemaResource: read serpapi://engines/google_light → full JSON schema
       - Use os.CreateTemp and filepath.Join for test fixtures
       - All test functions must have type annotations
  </action>
  <read_first>
    - internal/engines/engines.go (current stub)
    - legacy/src/server.py (lines 27-77, Python engine loading reference)
    - engines/google_light.json (engine JSON schema structure)
    - cmd/serpapi-mcp/main.go (from Plan 01)
    - internal/server/server.go (from Plan 01, for MCPServer type)
  </read_first>
  <acceptance_criteria>
    - internal/engines/engines.go contains "func LoadAndRegister" with signature returning (int, error)
    - internal/engines/engines.go contains in-memory map storing engine schemas
    - internal/engines/engines.go validates JSON files with fail-fast on corrupt/missing
    - internal/engines/engines.go skips engine filenames not matching [a-z0-9_]+
    - internal/engines/engines.go registers serpapi://engines index resource on MCP Server
    - internal/engines/engines.go registers per-engine resources serpapi://engines/{name}
    - cmd/serpapi-mcp/main.go calls engines.LoadAndRegister
    - cmd/serpapi-mcp/main.go has --engines-dir CLI flag
    - Startup log includes engine count
    - go test ./internal/engines/... passes
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/agenthands/serpapi-mcp && go build ./cmd/serpapi-mcp && go test ./internal/engines/... -count=1 -timeout 30s</automated>
  </verify>
  <done>All 107 engine schemas loaded at startup with validation, serpapi://engines lists all engines, serpapi://engines/{engine} returns individual schemas, server fails to start on corrupt/missing engine JSON</done>
</task>

<task type="auto">
  <name>Task 2: Integration test — full server startup with engines and healthcheck</name>
  <files>internal/server/server_test.go, internal/engines/engines_test.go</files>
  <action>
    1. Add or update integration tests in `internal/server/server_test.go` to verify the complete server assembly:
       - TestServerStartsWithEngines: Create MCPServer, call LoadAndRegister with the project's real engines/ directory, verify it loads 107 engines without error
       - TestHealthEndpointWithAuthAndCORS: Start server, send GET /health, verify 200 response and CORS headers (this may already exist from Plan 01 — extend it)
       - TestMissingEnginesDirFailsStartup: Call LoadAndRegister with non-existent dir, verify it returns error

    2. Add engine resource serving tests that exercise the full MCP resource read path:
       - Create a helper that builds a minimal MCP server, loads test engines, and simulates resource reads
       - Test that serpapi://engines resource returns JSON with "count" and "engines" fields
       - Test that serpapi://engines/{name} returns the correct engine schema

    3. Verify the complete binary:
       - `go build ./cmd/serpapi-mcp` succeeds
       - Running the binary with `--engines-dir ./engines` starts without error
       - Engine count is logged at startup
       
    Note: The real engines/ directory has 107 JSON files. The startup integration test should test with a small temp directory, not require the full 107 engines (that would be a smoke test for manual verification).
  </action>
  <read_first>
    - internal/server/server.go (from Plans 01 and 02)
    - internal/engines/engines.go (from Task 1)
    - cmd/serpapi-mcp/main.go (from Plans 01 and 02, updated)
  </read_first>
  <acceptance_criteria>
    - internal/server/server_test.go contains integration test for server startup with engines
    - Test verifies 107 engines load from real engines/ directory (or a controlled test directory)
    - Test verifies missing engines dir causes startup failure
    - go build ./cmd/serpapi-mcp compiles
    - go test ./internal/server/... ./internal/engines/... -count=1 passes all tests
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/agenthands/serpapi-mcp && go build ./cmd/serpapi-mcp && go test ./internal/... -count=1 -timeout 30s</automated>
  </verify>
  <done>Full integration test confirming server starts with engine resources, fails fast on corrupt/missing engines, and serves engine schemas via MCP resource protocol</done>
</task>

</tasks>

<verification>
1. `go build ./cmd/serpapi-mcp` compiles without errors
2. `go vet ./...` passes
3. `go test ./internal/... -count=1` passes all tests
4. serpapi://engines resource returns list of all 107 engine names
5. serpapi://engines/{engine} returns individual engine JSON schema
6. Server fails to start when engines/ directory is missing or contains corrupt JSON
7. Startup log shows engine count
</verification>

<success_criteria>
- Engine schemas loaded from engines/*.json at startup (ENG-01)
- serpapi://engines resource returns all available engine names (ENG-02)
- serpapi://engines/{engine} resources return per-engine parameter schemas (ENG-03)
- Startup validation fails fast on corrupt or missing JSON (ENG-04)
- Engine schema generation remains via build-engines.py, not ported to Go (ENG-05)
</success_criteria>

<output>
After completion, create `.planning/phases/02-server-auth-engine-resources/02-03-SUMMARY.md`
</output>