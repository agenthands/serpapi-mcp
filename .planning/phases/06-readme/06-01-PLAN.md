---
phase: 06-readme
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: false
requirements:
  - READ-01
  - READ-02
  - READ-03

must_haves:
  truths:
    - "Visitor can read README.md and understand what the server does and who it's for"
    - "Visitor can follow the quickstart guide and have a running server in under 5 minutes"
    - "Visitor can find links to all detailed documentation files (ARCHITECTURE.md, INSTALL.md, USAGE.md)"
    - "README has exactly 3 badges (Go version, CI status, test coverage) and no VS Code badge or logo image"
    - "Tone is technical and factual throughout — no marketing language"
  artifacts:
    - path: "README.md"
      provides: "Project overview, dual-path quickstart, brief auth & search sections, doc links, license"
      contains: "mcp.serpapi.com"
  key_links:
    - from: "README.md Quickstart"
      to: "Hosted service"
      via: "mcp.serpapi.com URL in config snippet"
      pattern: "mcp\\.serpapi\\.com"
    - from: "README.md Quickstart"
      to: "Self-hosting"
      via: "Binary install command + link to INSTALL.md"
      pattern: "INSTALL\\.md"
    - from: "README.md"
      to: "ARCHITECTURE.md"
      via: "Documentation links section"
      pattern: "ARCHITECTURE\\.md"
---

<objective>
Rewrite README.md from stale Python content to accurate Go codebase documentation.

Purpose: Replace the outdated Python-era README (references uv, src/server.py, Docker) with a new README reflecting the Go rewrite. The README serves as the repo entry point — visitors land here first and need to understand the project and get started quickly.

Output: README.md with overview, dual-path quickstart, brief auth & search sections, doc links, and license.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-readme/06-CONTEXT.md
@README.md
@cmd/serpapi-mcp/main.go
@go.mod
@Makefile
@.github/workflows/ci.yml
@LICENSE
@ARCHITECTURE.md

<interfaces>
<!-- Key facts extracted from source files for accurate README content -->

From cmd/serpapi-mcp/main.go:
- CLI flags: --host (MCP_HOST, default 0.0.0.0), --port (MCP_PORT, default 8000), --cors-origins (MCP_CORS_ORIGINS, default *), --auth-disabled (MCP_AUTH_DISABLED, default false), --engines-dir (ENGINES_DIR, default engines)
- Version info: serpapi-mcp {version} (commit: {commit}, built: {date})

From go.mod:
- Module: github.com/agenthands/serpapi-mcp
- Go version: 1.25
- Sole dependency: github.com/modelcontextprotocol/go-sdk v1.5.0

From internal/server (via ARCHITECTURE.md):
- Auth: path-based /{KEY}/mcp (recommended), header-based Authorization: Bearer {KEY}
- Hosted URL pattern: https://mcp.serpapi.com/{KEY}/mcp
- Default engine: google_light
- Response modes: complete (default), compact

From .github/workflows/ci.yml:
- CI runs on push to main and PRs to main
- Steps: checkout, setup-go 1.25.x, golangci-lint, go vet, go test -race

From Makefile:
- Targets: test, test-race, cover, lint, vet

From LICENSE:
- MIT License, Copyright (c) 2025 SerpApi
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write new README.md</name>
  <files>README.md</files>
  <action>
**Replace the entire README.md** with a complete rewrite for the Go codebase. The current README references Python (uv, src/server.py, Python 3.13+, Docker) — all of that is stale.

Structure and content (per D-03, D-06, D-08, D-10):

1. **H1 header** — Text-only, no logo image (per D-08): `# SerpApi MCP Server`

2. **Three badges** (per D-06, D-07 — no VS Code badge):
   - Go version: `[![Go Version](https://img.shields.io/badge/go-1.25+-00ADD8.svg)](https://go.dev/)` 
   - CI status: `[![CI](https://github.com/agenthands/serpapi-mcp/actions/workflows/ci.yml/badge.svg)](https://github.com/agenthands/serpapi-mcp/actions/workflows/ci.yml)`
   - Test coverage: `[![Coverage](https://img.shields.io/badge/coverage-86%25-green.svg)]()`
   Use shields.io badge format. For coverage, link to `make cover` explanation and note this is approximate/updated manually since there's no coverage badge service.

3. **Overview paragraph** (per D-01, D-09, D-10): 2-3 sentences explaining:
   - What it is: Go-based MCP server exposing SerpApi search capabilities through a single HTTP endpoint
   - Who it's for: MCP client integrators (config snippets, auth) and Go developers/contributors (build from source, architecture)
   - Mention 100+ engines, single `search` tool, parameter discovery via MCP resources
   - Technical/factual tone — no adjectives like "powerful", "comprehensive", "easy"

4. **Quickstart** section (per D-01, D-02): Two paths side by side in subsections:

   **Hosted service:**
   - Link to SerpApi dashboard for API key
   - MCP client config snippet showing `"url": "https://mcp.serpapi.com/YOUR_SERPAPI_API_KEY/mcp"`
   - 1-2 lines, then "See [USAGE.md](USAGE.md) for detailed configuration"

   **Self-hosting:**
   - Binary download from GitHub releases, or `go install github.com/agenthands/serpapi-mcp@latest`
   - Run: `serpapi-mcp --host 0.0.0.0 --port 8000` (or env vars MCP_HOST, MCP_PORT)
   - MCP client config snippet showing `http://localhost:8000/YOUR_SERPAPI_API_KEY/mcp`
   - Brief mention: "See [INSTALL.md](INSTALL.md) for all installation methods"
   
   Minimal detail per D-02 — config snippet + 1-2 line run command, full details in INSTALL.md/USAGE.md.

5. **Authentication** section (per D-03, D-04): Brief inline summary:
   - Two methods: path-based `/{KEY}/mcp` (recommended) and `Authorization: Bearer {KEY}` header
   - 2-3 line example showing both methods
   - One sentence: "See [USAGE.md](USAGE.md) for authentication details."

6. **Search tool** section (per D-03, D-04): Brief inline summary:
   - Single `search` tool with `params` dict
   - Example JSON showing a basic search: `{"name": "search", "arguments": {"params": {"q": "search query"}}}`
   - Mention default engine is `google_light`, `mode` accepts `complete` (default) or `compact`
   - Engine parameter schemas available as MCP resources: `serpapi://engines` and `serpapi://engines/<engine>`
   - One sentence: "See [USAGE.md](USAGE.md) for full parameter reference and examples."

7. **Documentation links** section (per D-03): Bullet list:
   - [ARCHITECTURE.md](ARCHITECTURE.md) — Package layout, request flows, subsystem designs
   - [INSTALL.md](INSTALL.md) — Installation for all platforms
   - [USAGE.md](USAGE.md) — Configuration, MCP client integration, error reference

8. **License** section: `MIT License — see [LICENSE](LICENSE) for details.`

Key rules:
- No Features section (D-05)
- No Development section (D-05)  
- No Troubleshooting section (D-05)
- No Contributing section (D-05)
- No Docker references (project ships static binaries, no Docker)
- No Python references (uv, src/server.py, build-engines.py are not in README scope)
- Technical/factual tone throughout (D-10)
- INSTALL.md and USAGE.md don't exist yet — link to them anyway (Phase 7 creates them)
  </action>
  <verify>
    <automated>grep -c "uv " README.md && echo "FAIL: Python uv reference found" || echo "PASS: No Python uv reference"; grep -c "src/server.py" README.md && echo "FAIL: Python server reference found" || echo "PASS: No Python server reference"; grep -c "docker" README.md && echo "FAIL: Docker reference found" || echo "PASS: No Docker reference"; grep -c "ARCHITECTURE.md" README.md && echo "PASS: Architecture link exists"; grep -c "INSTALL.md" README.md && echo "PASS: Install link exists"; grep -c "USAGE.md" README.md && echo "PASS: Usage link exists"; grep -c "mcp.serpapi.com" README.md && echo "PASS: Hosted service URL exists"</automated>
  </verify>
  <done>
    README.md completely rewritten for Go codebase:
    - No references to Python (uv, src/server.py) or Docker
    - Three badges only (Go, CI, coverage), no VS Code badge, no logo in H1
    - Dual-path quickstart (hosted + self-hosted) with config snippets
    - Brief inline auth and search sections with links to USAGE.md
    - Documentation links to ARCHITECTURE.md, INSTALL.md, USAGE.md
    - Technical/factual tone throughout
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify README renders correctly and meets requirements</name>
  <what-built>
Complete README.md rewrite for the Go codebase with dual-path quickstart, brief auth and search sections, doc links, and three badges.
  </what-built>
  <how-to-verify>
1. Open README.md in a GitHub-compatible Markdown renderer (or push to a branch and view on GitHub)
2. Verify these requirements:
   - **READ-01**: Can you understand what the server does and who it's for from reading the overview?
   - **READ-02**: Can you follow the quickstart to get a running server in under 5 minutes? Both paths (hosted and self-hosted)?
   - **READ-03**: Can you find links to ARCHITECTURE.md, INSTALL.md, and USAGE.md?
3. Check tone is technical/factual (no marketing adjectives)
4. Confirm exactly 3 badges (Go, CI, coverage) and no VS Code badge or logo
5. Verify no references to Python, uv, Docker, or src/server.py
6 </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
  <action>Present README.md to user for visual and content review against READ-01, READ-02, READ-03 requirements</action>
  <done>User confirms README renders correctly, all three requirements met, no stale content</done>
</task>

</tasks>

<verification>
1. README.md contains no Python/Docker references
2. Three badges present, no VS Code badge, no logo image
3. Dual-path quickstart with config snippets for both hosted and self-hosted
4. Auth section shows both path and header methods
5. Search tool section shows example and mentions resources
6. Documentation links section references all three docs
7. Tone is technical/factual throughout
8. Links to INSTALL.md and USAGE.md present (even though those files don't exist yet)
</verification>

<success_criteria>
- READ-01: Overview section explains what the server does and who it's for
- READ-02: Quickstart guide enables running server in under 5 minutes via either path
- READ-03: All three documentation links (ARCHITECTURE.md, INSTALL.md, USAGE.md) are present
- No stale Python/Docker content remains
- Three badges only (Go version, CI status, test coverage)
</success_criteria>

<output>
After completion, create `.planning/phases/06-readme/06-01-SUMMARY.md`
</output>